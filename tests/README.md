# Test Suite

This directory contains custom PHP test scripts to verify the functionality of the application.

## Running Tests

To run the test suite, execute the following command from the project root:

```bash
php tests/test_functions.php
```

## Structure

*   `test_helper.php`: Contains helper functions for assertions and test execution.
*   `test_functions.php`: Tests for `includes/functions.php`.
